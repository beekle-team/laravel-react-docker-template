name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# 同じ ref で新しい push があったら古い実行は打ち切る
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./src

    strategy:
      fail-fast: true
      matrix:
        # PR は本命の 8.4 だけで回し、テンプレートが約束する両バージョン互換は
        # main への push (マージ時) で検証する (Actions 消費の削減。PR 時点の
        # 8.3 退行はマージ後の main CI で検知される)。
        php: ${{ github.event_name == 'pull_request' && fromJSON('["8.4"]') || fromJSON('["8.3", "8.4"]') }}

    name: Lint (PHP ${{ matrix.php }})

    steps:
    - uses: actions/checkout@v7

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php }}
        extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv
        coverage: none

    - name: Install Composer Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

    - name: Execute Pint (Linting)
      run: ./vendor/bin/pint --test

    - name: Execute Larastan (Static Analysis)
      run: ./vendor/bin/phpstan analyse --memory-limit=2G

  frontend:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./src

    name: Frontend Quality

    steps:
    - uses: actions/checkout@v7

    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: src/package-lock.json

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.4
        extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv
        coverage: none

    - name: Install Composer Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

    - name: Install NPM Dependencies
      run: npm ci

    - name: Generate Wayfinder Routes
      run: php artisan wayfinder:generate --with-form

    - name: Execute Biome
      run: npm run lint:js

    - name: Execute TypeScript
      run: npm run types

    - name: Execute React Compiler Check
      run: npm run lint:react-compiler

    - name: Execute Dead Code Scan
      run: npm run lint:dead-code

    - name: Execute Duplication Scan
      run: npm run lint:duplication

    - name: Execute Frontend Unit Tests
      run: npm run test:unit

  tests:
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./src

    # Service container for PostgreSQL
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: testing
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    strategy:
      fail-fast: true
      matrix:
        # PR は本命の 8.4 だけで回し、テンプレートが約束する両バージョン互換は
        # main への push (マージ時) で検証する (Actions 消費の削減。PR 時点の
        # 8.3 退行はマージ後の main CI で検知される)。
        php: ${{ github.event_name == 'pull_request' && fromJSON('["8.4"]') || fromJSON('["8.3", "8.4"]') }}

    name: Tests (PHP ${{ matrix.php }})

    steps:
    - uses: actions/checkout@v7

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: ${{ matrix.php }}
        extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, pgsql, pdo_pgsql, bcmath, soap, intl, gd, exif, iconv
        coverage: none

    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: src/package-lock.json

    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"

    - name: Install Composer Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

    - name: Install NPM Dependencies
      run: npm ci

    - name: Generate Key
      run: php artisan key:generate

    - name: Build Frontend Assets
      run: npm run build

    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache

    - name: Execute Tests (Unit and Feature)
      env:
        DB_CONNECTION: pgsql
        DB_HOST: 127.0.0.1
        DB_PORT: 5432
        DB_DATABASE: testing
        DB_USERNAME: postgres
        DB_PASSWORD: password
      run: php artisan test

  e2e:
    runs-on: ubuntu-latest

    # ブラウザ導入やサーバ起動で詰まったときに走り続けないよう上限を切る
    timeout-minutes: 20

    defaults:
      run:
        working-directory: ./src

    name: E2E (Playwright)

    steps:
    - uses: actions/checkout@v7

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.4
        extensions: dom, curl, libxml, mbstring, zip, pcntl, pdo, sqlite, pdo_sqlite, bcmath, soap, intl, gd, exif, iconv
        coverage: none

    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: src/package-lock.json

    - name: Copy .env
      run: php -r "file_exists('.env') || copy('.env.example', '.env');"

    - name: Install Composer Dependencies
      run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist

    - name: Install NPM Dependencies
      run: npm ci

    - name: Generate Key
      run: php artisan key:generate

    - name: Prepare Database
      run: |
        touch database/database.sqlite
        php artisan migrate --force

    - name: Build Frontend Assets
      run: npm run build

    - name: Directory Permissions
      run: chmod -R 777 storage bootstrap/cache

    # Playwright の webServer が php artisan serve を起動するため、ここではブラウザだけ用意する。
    # 3 ブラウザ分は重いので CI は chromium に絞る。--with-deps は付けない
    # （runner に必要な共有ライブラリが揃っており、apt 経由の導入で待たされることがある）。
    - name: Install Playwright Browser
      run: npx playwright install chromium

    - name: Execute E2E Tests
      run: npx playwright test --project=chromium

    - name: Upload Playwright Report
      if: failure()
      uses: actions/upload-artifact@v7
      with:
        name: playwright-report
        path: src/playwright-report
        retention-days: 7
